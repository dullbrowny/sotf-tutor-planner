# Node / tooling
node_modules/
dist/
build/
.cache/
.eslintcache
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Vite
.vite/
**/.vite/

# Env & local (keep examples checked in)
.env*
!.env.example
!.env.local.example

# OS / editors
.DS_Store
*.swp
*~
.idea/
.vscode/
.history/

# Vercel
.vercel/

# Tests & coverage
coverage/
.nyc_output/
.jest/
.vitest/

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/
**/ms-playwright/

# Cypress (since you’ve run it before)
cypress/videos/
cypress/screenshots/
cypress/downloads/

# Storybook (if you spin it up later)
storybook-static/

# Turbo / monorepo caches (no-op if unused)
.turbo/

# Misc package managers (ensure only one lockfile lives here)
# (Keep package-lock.json committed)
# yarn.lock
# pnpm-lock.yaml

